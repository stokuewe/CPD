name: CI (Windows, placeholder)

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Show Python version
        run: python -V
      # - name: Install project (placeholder - uncomment when ready)
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install .[test]
      # - name: Run tests (placeholder)
      #   run: pytest -q

